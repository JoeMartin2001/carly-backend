syntax = "proto3";

package user;

service UserService {
    rpc FindOne (FindOneById) returns (User) {}
}

message FindOneById {
    string id = 1;
}

message User {
    string id = 1;
    string name = 2;
    string email = 3;
}